with container.Array
/**
 * Test that constructors may take Block parameters.
 */

type T {
  test "usage" {
    var i = 0
    val arr = new Array[int](3, ++i)

    6 == arr.fold(0) l,r do (l+r)
  }
}