/**
 * This test should show a Tyr equivalent of C's static local variables.
 */
type T {

  private[f] type var x = 0

  type def f = ++x

  // not allowed:
  type def g = ++x

  test "use" {
    0 == f() && 1 == f()
  }
}
