/**
 * This test shows tagged unions
 */
// make zero visible as single identifier
with other.zero

enum union <: mixed {
  if one int
  // not allowed, all need to be constants from mixed
  if zero int
  else void
}