/**
 * @note tests correctness of .type and new for ints
 */
type T {
  test "basic" {
    // a correct implementation of this expression does not involve a "1" as
    // member of a block
    0 == new ((new 7.type) + 1).type
  }
}
